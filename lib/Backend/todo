run command to use openGL/canavskit skia
flutter build web --web-renderer canvaskit --release

max width for cards -- edit Max Widget

offline persistance with mysql instead of firebase

refractor all

color?

Tanks   
    total fuel vs per tank toggle